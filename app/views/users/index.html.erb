<%= react_component("App", {mainContentType: "Users", users: @users, current_user: UserSerializer.new(current_user), authenticity_token: form_authenticity_token, flash: flash[:alert], followable_users: get_followable_users()} ) %>
