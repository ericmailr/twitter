<%= react_component("App", {mainContentType: @main_content_type, current_user: UserSerializer.new(current_user), posts: @posts, user: UserSerializer.new(@user), postTypes: @post_types, followable_users: get_followable_users()} ) %>
